; Portfolio Application NPM Configuration
; ==========================================

; Use legacy peer deps handling to avoid conflicts
legacy-peer-deps=true

; Set npm loglevel
loglevel=warn

; Save dependencies to package.json by default
save=true

; Use exact versions when installing
save-exact=false

; Show detailed output
; verbose=true
